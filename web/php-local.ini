; Reference only — `php -c` replaces the whole php.ini and can break extensions.
; Prefer: ./serve-local.sh
; or: php -d upload_max_filesize=50M -d post_max_size=60M -S 127.0.0.1:8000 ...

upload_max_filesize = 50M
post_max_size = 60M
